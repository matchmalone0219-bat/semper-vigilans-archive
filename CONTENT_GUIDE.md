# Semper Vigilans 内容更新规范

本指南用于指导《新蝙蝠侠2》档案站（Semper Vigilans Archive）的内容录入与日常资料维护，确保数据可靠性与结构一致性。

---

## 核心录入原则

0. **站点身份与品牌层级保持一致**：
   - Semper Vigilans 是 Batman小站（BatcaveCN）旗下《新蝙蝠侠2》专题档案，不再以独立“非官方中文档案库”作为前台主身份。
   - 首页固定品牌层级：`BATCAVECN SPECIAL ARCHIVE · THE BATMAN PART II` → `Semper Vigilans` → `Batman小站 ·《新蝙蝠侠2》专题档案`。
   - 页脚站点描述固定为 `Batman小站《新蝙蝠侠2》专题档案 · Semper Vigilans`，并保留与 Warner Bros.、DC Studios 及电影主创团队无官方合作关系的非商业影迷项目声明。
   - 涉及专题自建内容时，优先使用“Semper Vigilans 专题 / Semper Vigilans 扩展 / 专题适配”等明确主体，避免用“本站”造成 Batman小站母站与专题站之间的归属歧义。
   - 母站入口指向 `https://www.batcavecn.com/`；专题站内部导航、内容结构与视觉品牌保持独立。

1. **数据与展现分离**：内容数据应优先放在 `src/data/`（如 `film.ts`、`interviews.ts`、`recap.ts`）或稳定的实体库（如 `src/lib/people.ts`、`src/lib/places.ts`、`src/lib/relations.ts`）。
2. **禁止在组件中硬编码**：页面组件仅负责渲染与交互，严禁在 React 组件内硬编码新增新闻、路透或图文资料。
3. **拍摄日志结构约束**：新增拍摄日志必须严格遵循当前 `LOG` 数据结构（包含 `id`、`date`、`iso`、`title`、`body`、`kind`，可选 `source`、`sourceUrl`、`sourceTier`、`image`、`images`、`video` 等）。
   - 日志 `id` 是永久锚点，须唯一并以 `log-` 开头；更新标题、日期或排序时保留原 ID，同一天多条记录使用不同后缀。
4. **日期字段规范**：
   - `iso` 字段必须遵循 `YYYY-MM-DD` 格式且必须为有效日历日期（如平年 2 月 29 日、2 月 30 日等均会被校验器拦截）。
   - 展示日期 `date` 遵循项目约定（如 `YYYY.MM.DD` 或无明确日期时的 `YYYY.MM`）。
5. **来源信息三要素完整性**：若声明信源，以下三个字段必须同时提供，不可残缺：
   - `source`：信源机构或记者/账号名称（如 `Variety`、`STV News`、`片场路透 · hoeBread36`）。
   - `sourceUrl`：可访问的完整链接（必须以 `http://` 或 `https://` 开头）。
   - `sourceTier`：信源等级。
6. **来源等级按「实际链接的页面」分类，不按「这条证据在讲什么」**：
   - `official`：一手官方页面（导演/主创账号、制片厂公报、品牌官方商城、DC.com）。
   - `press`：新闻媒体页面。IGN 即使报道片场照片，链接本身仍是 `press`。
   - `set`：一手片场目击、现场社媒、交通管制记录（如 X 路透原帖、市政封路通告原文）。
   - `archive`：收藏实物、历史商品页、二手存档、藏家实拍。
   - 点开徽章后的页面必须与等级一致。媒体域名不得标 `official` / `set` / `archive`。
7. **静态资源存放规范**：本地图片与静态媒体统一存放在 `public/media/` 目录下（如 `public/media/log/`、`public/media/places/`）。
8. **路径与文件存在性保证**：数据中引用 `/media/...` 路径时，必须保证文件真实存在于本地对应目录中。
9. **外键关联 ID 校验**：
   - `CAST` 中的 `personId` 必须引用已有的人物节点 ID（存在于 `NODES`）。
   - 人物档案中的 `places` 必须引用已有的地点 ID（存在于 `PLACES`）。
   - 地点档案中的 `people` 必须引用已有的人物 ID。
   - 人物关系网络 `EDGES` 中的 `a` 与 `b` 必须引用已有的人物 ID。
10. **真实度与确信度等级**：故事线索及设定必须维持当前确信度规则：
    - `confirmed`：主创或官方正式公布。
    - `hint`：片场实景道具、拍摄通告等间接线索印证。
    - `rumor`：未经证实的行业或粉丝传闻推测。
    - `debunked`：曾经真实流传、后被可靠信息推翻。保留原始传闻与证伪依据；条目必须有稳定 `id`。
11. **访谈引言客观直译规范（严禁文学化改写）**：
    - 当事人直接引语（如 `quoteZh`）必须严格对照英文原文（`quoteEn`）直接翻译，忠实保留当事人原本平实、口语化的表达习惯（如“很有野心”、“一次‘急转弯’（real left turn）”、“剧本非常密（incredibly dense）”）。
    - 严禁脑补过度文学化的修辞拔高、分析性解释或主观修饰（如“另一重全新维度的探索”、“出人意料的急转弯”、“沉浸气质”、“格局宏大得令人赞叹”等）。
    - 若无法获得可逐字核对的原文，或现有资料仅支持二手转述/概括，则必须降级为“发言摘要 / remark summary”，不得使用引号、Quote 图标或“原话”措辞伪装成逐字引语。
12. **访谈及一手发言的分析性评述与直接引语结构分离**：
    - 本条适用于访谈、直接引语及其他以一手发言为主体的条目。剧情走向、伏笔联想、风格定调、背景解释等二级推论不得混入 `quoteZh` / 直接引语本身，应放入独立的 `note` / 按语字段。
    - 数据层必须保持“原话 / 翻译 / 按语”分层；前台依靠版式与字段位置区分，不强制显示 `【本站整理】`、`[ARCHIVE CURATOR NOTE]`、“经核对”等自证式标签。严谨性由来源结构、字段边界和校验规则保证，不靠反复声明。
    - 剧情线索、案件、人物关系、艺术溯源等本身以分析为主体的栏目，可在其既有结构内撰写分析正文；无需强行迁移到不存在的 `note` 字段，但仍须区分事实、线索、传闻与推论。
13. **史料原版与本站扩展彻底解耦（数据与标签两立）**：
    - 官方历史原版（`historical`）：素材或文本本身必须确属官方当年真实发布的历史原物（如 GCPD 嫌犯通缉素描三连图、官方密码本、GOOD BYE <?>）。严禁把普通电影剧照、本站复刻、后期拼接或自创内容挂上“官方原版”标签。
    - `historical` 描述的是**素材本身的历史真实性**，与 `sourceTier` 是两个维度。官方历史素材可以由可靠媒体、网页存档或历史报道佐证，因此其当前验证链接可能是 `press` / `archive`，并不因此失去 `historical` 属性。
    - 专题扩展（`extended`）：为连贯游玩或致敬而补充的线索、剧照、彩蛋，必须明确标为扩展或适配（如 `专题剧照适配 / Archive Adaptation`、`专题致敬扩展 / Archive Fan Tribute`）。
    - 对 Rataalada 等复原/互动内容，本站新建的数据对象默认视为 `extended`；只有在已核对历史原物或原文后，才允许显式标记为 `historical`。禁止通过 helper 默认值把新建内容自动升级为历史原版。
    - 数据对象必须独立分立，禁止在一个数据对象内将官方历史原文与自创彩蛋混合打包（例如 `GOODBYE.TXT` 与 `TRIBUTE.TXT` 必须分立）。
14. **核心事实采用单一权威数据源，并保证全站一致**：
    - 影片片名、正式档期等核心事实不得在多个模块各自维护一套独立值。对于《新蝙蝠侠 2》，`src/data/film.ts` 中的 `FILM`（如 `releaseLabel` / `releaseIso`）作为当前项目级权威事实源；其他页面、时间线、彩蛋与终端说明应引用或至少与其保持一致。
    - 当前正式北美公映档期为 **2028 年 2 月 18 日**，此处仅作为当前值示例；若官方信息未来调整，应优先更新权威事实源，再同步受影响的历史说明与展示文本，禁止只修改某一页面形成内部矛盾。
15. **前台文案保持档案叙事，不得暴露内部核验过程**：
    - `verifiedAt`、来源等级、确信度、provenance、审核状态等属于数据治理信息，可用于校验、排序和证据管理，但不得自动转写成“经核对”“本站确认”“资料显示”“已验证”“核验于……”等前台措辞。
    - 当条目存在不确定性时，应通过既有结构表达，例如 `confirmed / hint / rumor / debunked`、来源徽章、`historical / extended`、具体来源链接，而不是在正文末尾重复添加免责声明。
    - 禁止为了显得严谨而重复使用以下自证/找补句式：
      - “经核对……”
      - “本站判断 / 本站预计 / 本站整理……”
      - “官方尚未确认……”
      - “最终以成片为准……”
      - “资料显示……”
      - “已核验 / verified……”
      - “传闻均已标明出处……”
    - 如果“不确定”本身就是重要事实，可直接描述客观状态，例如：“角色尚未公布”“身份仍未公开”“影迷由此联想到无政府客”，无需追加自我免责句。
    - **例外**：法律/版权性质的站点身份声明、明确的 `RUMOR / DEBUNKED / CONFIRMED` 状态标签、来源类型标签、官方与本站扩展的 provenance 区分必须保留。
    - **核心原则**：禁止词表仅用于发现潜在问题，不代替语义判断；描述新闻事实中的“官方确认 / 否认”可以保留，描述本站编辑行为的“经核对 / 本站确认 / 已验证”原则上不得出现在前台。

---

## 验证工作流

新增资料或修改数据后，请依次执行以下命令：

1. **内容校验（必须通过）**：
   ```bash
   npm run content:check
   ```
   该命令会自动核查日期合法性、信源三要素、媒体文件存在性以及关联实体 ID。

2. **双语覆盖校验（必须通过）**：
   ```bash
   npm run i18n:check
   ```
   新增日志、人物和其他档案时检查对应英文条目；PR 检查及 GitHub Pages 部署均执行此项。

3. **图片资产检查**：
   ```bash
   npm run media:check
   ```
   命令会报告重复图、孤儿图、过大/过小图和 MIME 错配。重复图、孤儿图与 MIME 错配同时纳入 `npm test` 回归保护，提交前必须清零；尺寸过小或文件偏大继续作为人工优化提示，不自动删除。

4. **完整提交前验证（建议执行）**：
   ```bash
   npm run typecheck
   npm run test
   npm run content:check
   npm run i18n:check
   npm run media:check
   npm run build:github-pages
   ```

---

## 强调事项

新增普通资料时，原则上只应修改 `src/data/` 和 `public/media/`。如发现每次新增一条资料都需要修改 React 页面，应优先检查是否存在结构设计问题。
